import pytest\nimport asyncio\nfrom datetime import datetime\nfrom app.models import Message, HoneypotRequest, Metadata\nfrom app.agents.scam_detector import ScamDetector\nfrom app.agents.conversation_agent import ConversationAgent\nfrom app.services.intelligence_extractor import IntelligenceExtractor\n\n@pytest.fixture\ndef sample_scam_message():\n    return Message(\n        sender=\"scammer\",\n        text=\"Your bank account will be blocked today. Verify immediately by sharing your OTP.\",\n        timestamp=datetime.utcnow()\n    )\n\n@pytest.fixture\ndef sample_legitimate_message():\n    return Message(\n        sender=\"user\",\n        text=\"Hello, how are you doing today?\",\n        timestamp=datetime.utcnow()\n    )\n\n@pytest.mark.asyncio\nasync def test_scam_detection_positive(sample_scam_message):\n    detector = ScamDetector()\n    result = await detector.analyze_message(sample_scam_message, session_id=\"test-123\")\n    \n    assert result.is_scam == True\n    assert result.confidence > 0.5\n    assert result.scam_type is not None\n\n@pytest.mark.asyncio\nasync def test_scam_detection_negative(sample_legitimate_message):\n    detector = ScamDetector()\n    result = await detector.analyze_message(sample_legitimate_message, session_id=\"test-456\")\n    \n    assert result.is_scam == False\n    assert result.confidence < 0.7\n\ndef test_intelligence_extraction():\n    extractor = IntelligenceExtractor()\n    \n    messages = [\n        Message(\n            sender=\"scammer\",\n            text=\"Please share your UPI ID scammer@paytm for verification. Call 9876543210.\",\n            timestamp=datetime.utcnow()\n        ),\n        Message(\n            sender=\"scammer\",\n            text=\"Visit https://suspicious-bank-verify.com to complete process.\",\n            timestamp=datetime.utcnow()\n        )\n    ]\n    \n    intelligence = extractor.extract_from_conversation(messages)\n    \n    assert len(intelligence.upiIds) > 0\n    assert len(intelligence.phoneNumbers) > 0\n    assert len(intelligence.phishingLinks) > 0\n    assert len(intelligence.suspiciousKeywords) > 0\n\nif __name__ == \"__main__\":\n    pytest.main([\"-v\", __file__])